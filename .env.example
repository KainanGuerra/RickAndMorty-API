# Copy to .env and adjust as needed. Used by docker-compose.yml.

POSTGRES_USER=rickandmorty
POSTGRES_PASSWORD=rickandmorty # placeholder for local dev only, override in your own .env
POSTGRES_DB=rickandmorty

JWT_SECRET=changeme # placeholder, override with a random value in your own .env
JWT_EXPIRES_IN=1h

RICK_AND_MORTY_API_URL=https://rickandmortyapi.com/api

BACKEND_PORT=3001
FRONTEND_PORT=3000
